# Copy to .env.local for local dev (gitignored — Next.js's own convention,
# distinct from the backend's .env). NEXT_PUBLIC_ prefix is required for
# Next.js to expose a var to browser-side code — this one isn't a secret
# (it's just where the browser sends API requests), so that's fine here.
NEXT_PUBLIC_API_URL=http://localhost:8000
